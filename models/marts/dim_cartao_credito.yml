version: 2

models:
  - name: dim_cartao_credito
    description: "Tabela de cartões de crédito."
    columns:
      - name: 'codigo_cartao'
        tests:
          - unique:
              severity: error
          - not_null:
              severity: error