version: 2

models:
  - name: dim_produtos
    description: "Tabela de produtos."
    columns:
      - name: 'codigo_produto'
        tests:
          - unique:
              severity: error
          - not_null:
              severity: error