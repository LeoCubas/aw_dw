version: 2

models:
  - name: dim_enderecos
    description: "Tabela de endereços."
    columns:
      - name: 'codigo_endereco'
        tests:
          - unique:
              severity: error
          - not_null:
              severity: error