version: 2

models:
  - name: dim_motivos_vendas
    description: "Tabela de Motivos de Vendas."
    columns:
      - name: 'codigo_motivo_venda'
        tests:
          - unique:
              severity: error
          - not_null:
              severity: error