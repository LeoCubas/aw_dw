version: 2

models:
  - name: dim_vendedores
    description: "Tabela de vendedores."
    columns:
      - name: 'codigo_vendedor'
        tests:
          - unique:
              severity: error
          - not_null:
              severity: error