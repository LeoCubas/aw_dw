version: 2

models:
  - name: dim_clientes
    description: "Tabela de clientes."
    columns:
      - name: 'codigo_cliente'
        tests:
          - unique:
              severity: error
          - not_null:
              severity: error